# Base URL for the Veloop Backend API
# Leave empty or relative '/api' if using a proxy, or set to full URL:
VITE_API_BASE_URL=http://localhost:5000/api

# Set to 'true' to force mock mode during development without a running backend server
VITE_USE_MOCK_API=false
